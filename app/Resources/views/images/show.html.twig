{% extends 'layout.html.twig' %}

{% block contenido %}
    <h2>Imagen</h2>

    <img class="center-block" src="{{ asset('assets/images/galeries/') ~ image.image }}" alt="Imagen" width="50%">

    <a class="btn" href="{{ path('images_index') }}">Volver</a>

    {{ form_start(delete_form) }}
    <p></p>
    <input class="btn" type="submit" value="Eliminar">
    <p></p>
    {{ form_end(delete_form) }}
{% endblock %}
