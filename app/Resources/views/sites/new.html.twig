{% extends 'layout.html.twig' %}

{% block contenido %}
    <h2>Creación de sitios de interés</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <p></p>
        <input class="btn" type="submit" value="Crear" />
    <a class="btn" href="{{ path('routes_new') }}">Volver</a>
    {{ form_end(form) }}
    <p></p>
{% endblock %}
